import { LoggermilddwareMiddleware } from './loggermilddware.middleware';

describe('LoggermilddwareMiddleware', () => {
  it('should be defined', () => {
    expect(new LoggermilddwareMiddleware()).toBeDefined();
  });
});
